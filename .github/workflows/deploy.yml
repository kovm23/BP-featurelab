name: Deploy to production

on:
  push:
    branches: [prod]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Build frontend
        working-directory: microsoft-edu-ai-hackathon-2025/frontend
        run: npm ci && npm run build
        env:
          VITE_API_BASE: /api

      - name: Deploy frontend
        run: |
          sudo rsync -az --delete \
            microsoft-edu-ai-hackathon-2025/frontend/dist/ \
            /var/www/llmfeatures/current/
          sudo chown -R www-data:www-data /var/www/llmfeatures

      - name: Reload backend
        run: |
          cd /home/kovm23/BP-featurelab/microsoft-edu-ai-hackathon-2025/backend
          git pull --ff-only origin prod
          npx --yes pm2 reload ecosystem.config.js --only backend --update-env
