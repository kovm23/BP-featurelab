# Full local profile: shared backend stack + GPU-only extras for Whisper STT.
# The shared/pinned dependencies live in requirements-base.txt.
-r requirements-base.txt

# GPU-only extras (audio transcription via faster-whisper). NOT exercised by CI
# (no GPU runner); validate on the GPU host. Versions are numpy-2.x compatible.
torch==2.5.1
torchaudio==2.5.1
torchvision==0.20.1
faster-whisper>=1.0.0
