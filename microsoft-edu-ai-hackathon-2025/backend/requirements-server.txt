# Server deployment requirements — no local GPU.
# torch / faster-whisper are intentionally excluded: Whisper needs a local GPU
# which this server does not have. Audio transcription is gracefully skipped.
# Ollama (LLM) runs remotely on llm.vse.cz.
# All shared/pinned dependencies live in requirements-base.txt.
-r requirements-base.txt
